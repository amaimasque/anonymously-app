import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#9BB8CD",
    },
    secondary: {
      main: "#FFF7D4",
    },
    background: {
      default: "#EEC759",
      paper: "#FFF7D4",
    },
  },
});

export { theme };
